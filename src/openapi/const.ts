import { Enum } from "#json-editor/utils";

export const K = Enum(
  "servers",
  "paths",
  "webhooks",
  "components",
  "security",
  "tags",
  "info",

  "securitySchemes",

  "required",
  "enum",
  "$ref",

  "flows",
  "examples",
  "example",
  "responses",
  "parameters",
  "requestBody",
  "content",
  "schema",
  "properties",
  "items",
);
